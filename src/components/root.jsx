import React from 'react';
import App from './app'

const Root = ({ store }) => { 
    return (
        // <Provider store={store}>
        //     <HashRouter>
        <App />
                /* <Landing /> */
            /* </HashRouter>
        </Provider> */
    )
};

export default Root;